[build]
  command = "npm run build -- --configuration production"
  publish = "dist/bus-tracker-ui/browser"

# SPA routing — redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
